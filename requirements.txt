# UI
gradio>=3.52,<4

# Core LangChain
langchain>=0.1.0,<1.3

# HuggingFace Transformers & LLM pipeline
transformers>=4.35,<5
torch>=2.1,<3

# Sentence Transformers for embeddings
sentence-transformers>=2.2.2,<3

# Vectorstore
faiss-cpu>=1.7.4,<2  # Use faiss-gpu if deploying on GPU

# Document parsing
unstructured>=0.7.10,<0.8
python-magic>=0.4.27,<0.5

# Optional: accelerate & bitsandbytes for large models / GPU optimization
accelerate>=0.21,<1
bitsandbytes>=0.41.0,<1
